language: python

python:
  - "3.7"
  - "3.6"

cache:
  pip: true
  directories:
    - "$HOME/.cache/pypoetry"

before_install:
  - pip install pip -U
  - pip install poetry

install:
  - poetry install -v

script: pytest -x tests

sudo: false
